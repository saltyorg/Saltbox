##########################################################################
# Title:         Saltbox: Light LDAP | Default Variables                 #
# Author(s):     salty                                                   #
# URL:           https://github.com/saltyorg/Saltbox                     #
# --                                                                     #
##########################################################################
#                   GNU General Public License v3.0                      #
##########################################################################
---
################################
# Basics
################################

lldap_name: "lldap"

################################
# Settings
################################

# Toggles if the configuration template uses SMTP or not.
lldap_role_smtp_enabled: false
# The SMTP server.
lldap_role_smtp_server: "smtp.gmail.com"
# The SMTP port.
lldap_role_smtp_port: "587"
# How the connection is encrypted, either "NONE" (no encryption), "TLS" or "STARTTLS".
lldap_role_smtp_encryption: "TLS"
# The SMTP user, usually your email address.
lldap_role_smtp_user: "sender@gmail.com"
# The SMTP password.
lldap_role_smtp_password: "password"
# is a free-form name, followed by an email between <>.
lldap_role_smtp_from: "LLDAP Admin <sender@gmail.com>"
# The header field, optional: how the sender appears in the email.
# The first is a free-form name, followed by an email between <>.
lldap_role_smtp_reply_to: "Do not reply <noreply@localhost>"

################################
# Paths
################################

lldap_role_paths_folder: "{{ lldap_name }}"
lldap_role_paths_location: "{{ server_appdata_path }}/{{ lldap_role_paths_folder }}"
lldap_role_paths_folders_list:
  - "{{ lldap_role_paths_location }}"

################################
# Web
################################

lldap_role_web_subdomain: "lldap"
lldap_role_web_domain: "{{ user.domain }}"
lldap_role_web_port: "17170"
lldap_role_web_url: "{{ 'https://' + (lookup('role_var', '_web_subdomain', role='lldap') + '.' + lookup('role_var', '_web_domain', role='lldap')
                     if (lookup('role_var', '_web_subdomain', role='lldap') | length > 0)
                     else lookup('role_var', '_web_domain', role='lldap')) }}"

################################
# DNS
################################

lldap_role_dns_record: "{{ lookup('role_var', '_web_subdomain', role='lldap') }}"
lldap_role_dns_zone: "{{ lookup('role_var', '_web_domain', role='lldap') }}"
lldap_role_dns_proxy: "{{ dns.proxied }}"

################################
# Traefik
################################

lldap_role_traefik_sso_middleware: "{{ traefik_default_sso_middleware }}"
lldap_role_traefik_middleware_default: "{{ traefik_default_middleware }}"
lldap_role_traefik_middleware_custom: ""
lldap_role_traefik_certresolver: "{{ traefik_default_certresolver }}"
lldap_role_traefik_enabled: true
lldap_role_traefik_api_enabled: false
lldap_role_traefik_api_endpoint: ""

################################
# Docker
################################

# Container
lldap_role_docker_container: "{{ lldap_name }}"

# Image
lldap_role_docker_image_pull: true
lldap_role_docker_image_repo: "nitnelave/lldap"
lldap_role_docker_image_tag: "stable"
lldap_role_docker_image: "{{ lookup('role_var', '_docker_image_repo', role='lldap')
                             + ':' + lookup('role_var', '_docker_image_tag', role='lldap') }}"

# Ports
lldap_role_docker_ports_defaults: []
lldap_role_docker_ports_custom: []
lldap_role_docker_ports: "{{ lookup('role_var', '_docker_ports_defaults', role='lldap')
                             + lookup('role_var', '_docker_ports_custom', role='lldap') }}"

# Envs
lldap_role_docker_envs_default:
  TZ: "{{ tz }}"
  UID: "{{ uid }}"
  GID: "{{ gid }}"
lldap_role_docker_envs_custom: {}
lldap_role_docker_envs: "{{ lookup('role_var', '_docker_envs_default', role='lldap')
                            | combine(lookup('role_var', '_docker_envs_custom', role='lldap')) }}"

# Commands
lldap_role_docker_commands_default: []
lldap_role_docker_commands_custom: []
lldap_role_docker_commands: "{{ lookup('role_var', '_docker_commands_default', role='lldap')
                                + lookup('role_var', '_docker_commands_custom', role='lldap') }}"

# Volumes
lldap_role_docker_volumes_default:
  - "{{ lldap_role_paths_location }}:/data"
lldap_role_docker_volumes_custom: []
lldap_role_docker_volumes: "{{ lookup('role_var', '_docker_volumes_default', role='lldap')
                               + lookup('role_var', '_docker_volumes_custom', role='lldap') }}"

# Devices
lldap_role_docker_devices_default: []
lldap_role_docker_devices_custom: []
lldap_role_docker_devices: "{{ lookup('role_var', '_docker_devices_default', role='lldap')
                               + lookup('role_var', '_docker_devices_custom', role='lldap') }}"

# Hosts
lldap_role_docker_hosts_default: {}
lldap_role_docker_hosts_custom: {}
lldap_role_docker_hosts: "{{ docker_hosts_common
                             | combine(lookup('role_var', '_docker_hosts_default', role='lldap'))
                             | combine(lookup('role_var', '_docker_hosts_custom', role='lldap')) }}"

# Labels
lldap_role_docker_labels_default: {}
lldap_role_docker_labels_custom: {}
lldap_role_docker_labels: "{{ docker_labels_common
                              | combine(lookup('role_var', '_docker_labels_default', role='lldap'))
                              | combine(lookup('role_var', '_docker_labels_custom', role='lldap')) }}"

# Hostname
lldap_role_docker_hostname: "{{ lldap_name }}"

# Networks
lldap_role_docker_networks_alias: "{{ lldap_name }}"
lldap_role_docker_networks_default: []
lldap_role_docker_networks_custom: []
lldap_role_docker_networks: "{{ docker_networks_common
                                + lookup('role_var', '_docker_networks_default', role='lldap')
                                + lookup('role_var', '_docker_networks_custom', role='lldap') }}"

# Capabilities
lldap_role_docker_capabilities_default: []
lldap_role_docker_capabilities_custom: []
lldap_role_docker_capabilities: "{{ lookup('role_var', '_docker_capabilities_default', role='lldap')
                                    + lookup('role_var', '_docker_capabilities_custom', role='lldap') }}"

# Security Opts
lldap_role_docker_security_opts_default: []
lldap_role_docker_security_opts_custom: []
lldap_role_docker_security_opts: "{{ lookup('role_var', '_docker_security_opts_default', role='lldap')
                                     + lookup('role_var', '_docker_security_opts_custom', role='lldap') }}"

# Restart Policy
lldap_role_docker_restart_policy: unless-stopped

# State
lldap_role_docker_state: started
