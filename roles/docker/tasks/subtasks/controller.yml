#########################################################################
# Title:         Saltbox: Docker | Controller Tasks                     #
# Author(s):     salty                                                  #
# URL:           https://github.com/saltyorg/Saltbox                    #
# --                                                                    #
#########################################################################
#                   GNU General Public License v3.0                     #
#########################################################################
---
- name: Controller | Delete venv folder
  ansible.builtin.file:
    path: "{{ docker_controller_venv_path }}"
    state: absent

- name: Controller | Install sdc release
  ansible.builtin.include_tasks: "{{ role_path }}/tasks/subtasks/github_binary/install.yml"
  vars:
    docker_github_binary_install:
      name: sdc
      path: "{{ docker_controller_binary_path }}"
      download_url: "{{ docker_controller_download_url }}"
      checksum: "{{ docker_controller_release_checksum }}"
      update_required: "{{ docker_controller_update_required }}"

- name: Controller | Import 'saltbox_managed_docker_controller.service'
  ansible.builtin.template:
    src: docker-controller.service.j2
    dest: /etc/systemd/system/saltbox_managed_docker_controller.service
    mode: "0644"
    force: true

- name: Controller | Enable 'saltbox_managed_docker_controller.service'
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/systemd/manage_service.yml"
  vars:
    _service_name: saltbox_managed_docker_controller
    _service_enabled: true
    _service_state: restarted
    _service_daemon_reload: true
    _service_reenable: true

- name: Controller | Wait until Controller readiness route is available
  ansible.builtin.uri:
    url: "{{ docker_controller_url }}/ready"
    method: GET
    return_content: true
    status_code:
      - 200
      - 404
      - 503
  register: docker_controller_readiness
  until:
    - docker_controller_readiness.status is defined
    - docker_controller_readiness.status in [200, 404]
  retries: 120
  delay: 1

- name: Controller | Fall back to legacy Controller liveness route
  ansible.builtin.uri:
    url: "{{ docker_controller_url }}/ping"
    method: GET
    return_content: true
    status_code: 200
  register: docker_controller_legacy_readiness
  until: docker_controller_legacy_readiness.status == 200
  retries: 120
  delay: 1
  when: docker_controller_readiness.status == 404

- name: Controller | Block Docker Controller
  ansible.builtin.uri:
    url: "{{ docker_controller_url }}/block/20"
    method: POST
    timeout: 600

- name: Controller | Import 'saltbox_managed_docker_controller_helper.service'
  ansible.builtin.template:
    src: docker-controller-helper.service.j2
    dest: /etc/systemd/system/saltbox_managed_docker_controller_helper.service
    mode: "0644"
    force: true

- name: Controller | Enable 'saltbox_managed_docker_controller_helper.service'
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/systemd/manage_service.yml"
  vars:
    _service_name: saltbox_managed_docker_controller_helper.service
    _service_enabled: true
    _service_state: restarted
    _service_daemon_reload: true
    _service_reenable: true

- name: "Controller | Wait for 10 seconds before commencing"
  ansible.builtin.wait_for:
    timeout: "10"
